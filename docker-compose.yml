# AI Kids frontend only. All API traffic goes to StoryMee Hub.

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://dev-hub.storymee.com}
    ports:
      - '8080:80'
