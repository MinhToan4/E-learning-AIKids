# Public StoryMee Gateway only. Never point the browser at a core service port.
VITE_API_URL=https://dev-hub.storymee.com
# Optional for local full-stack development:
# VITE_API_PROXY_TARGET=http://127.0.0.1:5100

# Firebase is optional. Email/SMS/push delivery remains disabled until providers
# are configured on the backend.
