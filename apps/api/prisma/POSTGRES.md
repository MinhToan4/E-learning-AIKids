# PostgreSQL / Supabase

Canonical guide: **[docs/SUPABASE.md](../../../docs/SUPABASE.md)**

- Provider: `postgresql` only (`schema.prisma`)
- Connection: `DATABASE_URL` in `apps/api/.env` (server only)
- No local Docker Postgres required for daily development
