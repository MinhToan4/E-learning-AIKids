{
  "name": "e-learning-aikids-full",
  "private": true,
  "version": "1.0.0",
  "description": "Kids AI Creator Academy frontend for the StoryMee core platform",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @aikids/web",
    "dev:web": "npm run dev -w @aikids/web",
    "build": "npm run build -w @aikids/domain && npm run build -w @aikids/web",
    "test": "npm run test -w @aikids/domain && npm run test -w @aikids/web",
    "test:all": "npm test",
    "load:smoke": "node ./scripts/load-smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
